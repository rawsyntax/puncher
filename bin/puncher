#!/usr/bin/env ruby
require File.expand_path(File.dirname(__FILE__) + '/../lib/puncher')


if ARGV.length >= 2
  Puncher.new(ARGV.shift, ARGV)
else
  puts "How to use puncher:"
  puts "  puncher 'command' glob"
end
